using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Platidlo : MonoBehaviour {

	
	// Update is called once per frame
	void Update () {

        GameObject.Destroy(gameObject, 20f);
    }
}
